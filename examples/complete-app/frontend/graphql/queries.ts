/**
 * GraphQL Queries and Mutations
 * 
 * Import ShieldForge's pre-built GraphQL documents.
 */
import {
  LOGIN_MUTATION,
  REGISTER_MUTATION,
  LOGOUT_MUTATION,
  ME_QUERY,
  UPDATE_PROFILE_MUTATION,
  UPDATE_PASSWORD_MUTATION,
  REQUEST_PASSWORD_RESET_MUTATION,
  RESET_PASSWORD_MUTATION,
} from '@appforgeapps/shieldforge-graphql';

export {
  LOGIN_MUTATION,
  REGISTER_MUTATION,
  LOGOUT_MUTATION,
  ME_QUERY,
  UPDATE_PROFILE_MUTATION,
  UPDATE_PASSWORD_MUTATION,
  REQUEST_PASSWORD_RESET_MUTATION,
  RESET_PASSWORD_MUTATION,
};

/**
 * These are ready-to-use GraphQL documents from ShieldForge.
 * Use them directly with your GraphQL client (Apollo, urql, etc.)
 */
